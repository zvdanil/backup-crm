# Как выполнить все запросы в DBeaver

## ❌ Проблема

DBeaver по умолчанию выполняет только **один запрос за раз** (до точки с запятой `;`). Поэтому вы видите только последний результат.

## ✅ Решение 1: Использовать объединенный запрос (рекомендуется)

Я создал файл `cleanup_orphaned_data_report_single_query.sql` - это **один большой запрос**, который объединяет все проверки.

### Как использовать:

1. **Откройте SQL Editor** (Ctrl+`)
2. **Откройте файл:** `cleanup_orphaned_data_report_single_query.sql` (Ctrl+O)
3. **Выполните запрос:** F5 или Ctrl+Enter
4. **Просмотрите все результаты** в одной таблице

Этот файл содержит **один запрос** с `UNION ALL`, который покажет все категории сразу.

---

## ✅ Решение 2: Выполнить весь скрипт через "Execute SQL Script"

### Способ А: Через меню

1. **Откройте файл** `cleanup_orphaned_data_report.sql` в SQL Editor
2. В меню выберите: **"База даних"** (Database) → **"Execute SQL Script"**
3. Или нажмите кнопку **"Execute SQL Script"** на панели инструментов (обычно с иконкой "Play" или "Execute")

### Способ Б: Горячие клавиши

1. **Откройте файл** в SQL Editor
2. Нажмите **Ctrl+Shift+Enter** (выполнить весь скрипт)
3. Или **Alt+X** (в некоторых версиях DBeaver)

### Способ В: Через контекстное меню

1. **Правый клик** в SQL Editor
2. Выберите **"Execute SQL Script"** или **"Execute All"**

---

## ✅ Решение 3: Настроить DBeaver для выполнения всех запросов

1. В меню: **"Window"** → **"Preferences"** (или **"Налаштування"**)
2. Перейдите: **"SQL Editor"** → **"SQL Execution"**
3. Найдите опцию **"Execute queries in separate tabs"** или **"Execute all queries"**
4. Включите эту опцию
5. Нажмите **"OK"**

Теперь при нажатии F5 будут выполняться все запросы из скрипта.

---

## ✅ Решение 4: Выполнить запросы по одному

Если ничего не помогает, можно выполнить каждый запрос отдельно:

1. Откройте `cleanup_orphaned_data_report.sql`
2. Выделите **первый SELECT** (строки 10-14)
3. Нажмите **F5**
4. Запишите результат
5. Выделите **следующий SELECT** (строки 19-24)
6. Нажмите **F5**
7. И так далее...

---

## 🎯 Рекомендуемый способ

**Используйте файл `cleanup_orphaned_data_report_single_query.sql`:**

1. Откройте этот файл в SQL Editor
2. Нажмите **F5**
3. Вы увидите **все результаты** в одной таблице

Это самый простой и надежный способ!

---

## 📋 Разница между режимами выполнения

- **F5** или **Ctrl+Enter** - выполняет **текущий запрос** (до точки с запятой)
- **Ctrl+Shift+Enter** или **"Execute SQL Script"** - выполняет **весь скрипт**
- **Alt+X** - в некоторых версиях выполняет весь скрипт

---

## 💡 Совет

Если не получается выполнить весь скрипт, просто используйте файл `cleanup_orphaned_data_report_single_query.sql` - он содержит один большой запрос, который покажет все результаты сразу.
