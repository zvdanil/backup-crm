# Пошаговая инструкция для DBeaver

## 📦 Шаг 1: Создание бэкапа

### Способ 1: Через контекстное меню (самый простой)

1. **В левой панели "Навігатор баз даних"** найдите подключение `postgres`
2. **Правый клик** на `postgres` (на самом подключении, не на базе данных)
3. В меню выберите: **"Tools"** → **"Dump Database"** или **"Backup Database"**
4. Если этих пунктов нет, попробуйте: **"Tools"** → **"Export Data"**

### Способ 2: Через меню базы данных

1. **Раскройте** подключение `postgres` в левой панели (кликните на стрелку)
2. Найдите папку **"Databases"** → **"postgres"**
3. **Правый клик** на `postgres` (на базе данных)
4. Выберите **"Tools"** → **"Dump Database"** или **"Export Data"**

### Способ 3: Через меню "База даних" (Database)

1. В верхнем меню выберите **"База даних"** (Database)
2. Выберите **"Dump Database"** или **"Export Data"**
3. Выберите подключение и базу данных

---

## 📊 Шаг 2: Открытие SQL Editor

### Способ 1: Горячие клавиши (самый быстрый)

1. Нажмите **Ctrl+`** (обратная кавычка) или **Ctrl+Enter**
2. Откроется SQL Editor

### Способ 2: Через меню

1. В верхнем меню выберите **"Редагування SQL"** (SQL Editing)
2. Выберите **"SQL Editor"** или **"New SQL Script"**

### Способ 3: Через кнопку на панели инструментов

1. Найдите кнопку **"SQL"** на панели инструментов (обычно с иконкой SQL)
2. Нажмите на неё или на стрелку рядом
3. Выберите **"SQL Editor"** или **"New SQL Script"**

### Способ 4: Через контекстное меню

1. **Правый клик** на подключении `postgres` в левой панели
2. Выберите **"SQL Editor"** или **"New SQL Script"**

---

## 📁 Шаг 3: Открытие SQL файла

После открытия SQL Editor:

1. В SQL Editor нажмите **Ctrl+O** (или File → Open File)
2. Перейдите в папку проекта: `C:\Users\User\Downloads\crmbackup-cursor`
3. Выберите файл:
   - `cleanup_orphaned_data_report.sql` (для начала)
   - или `cleanup_orphaned_data.sql` (для очистки)
4. Нажмите **"Open"**

---

## ▶️ Шаг 4: Выполнение SQL скрипта

После открытия файла в SQL Editor:

1. **Убедитесь, что подключение правильное** (вверху SQL Editor должно быть указано `postgres`)
2. Нажмите **F5** (или кнопку "Execute SQL Script" на панели инструментов)
3. Или выберите весь текст (Ctrl+A) и нажмите **Ctrl+Enter**
4. Результаты появятся в нижней панели

---

## 🔍 Альтернатива: Выполнение SQL напрямую

Если не можете найти файлы:

1. Откройте SQL Editor (Ctrl+`)
2. Скопируйте содержимое файла `cleanup_orphaned_data_report.sql`
3. Вставьте в SQL Editor (Ctrl+V)
4. Нажмите F5 для выполнения

---

## 📋 Быстрая справка по интерфейсу

### Левая панель (Навігатор баз даних):
- Здесь отображаются все подключения к базам данных
- Ваше подключение: `postgres aws-1-eu-west-1.pooler.supabase.c`

### Центральная область:
- Здесь открываются SQL Editor, таблицы, данные
- Сейчас пустая - откройте SQL Editor

### Нижняя панель:
- "Files - General" - файлы проекта
- "postgres" - дашборд подключения

### Верхнее меню:
- **"База даних"** (Database) - операции с БД
- **"Редагування SQL"** (SQL Editing) - SQL Editor

---

## 🎯 Рекомендуемый порядок действий

1. **Откройте SQL Editor:**
   - Нажмите **Ctrl+`** или **Ctrl+Enter**
   - Или через меню: **"Редагування SQL"** → **"SQL Editor"**

2. **Откройте файл отчета:**
   - В SQL Editor: **Ctrl+O**
   - Выберите: `cleanup_orphaned_data_report.sql`

3. **Выполните скрипт:**
   - Нажмите **F5**
   - Просмотрите результаты

4. **Если нужно очистить:**
   - Откройте: `cleanup_orphaned_data.sql`
   - Выполните: **F5**

---

## 💡 Если не можете найти функции

**Для бэкапа:**
- Попробуйте: **Правый клик на подключении** → **"Tools"** → **"Export Data"**
- Или через меню: **"База даних"** → **"Export Data"**

**Для SQL Editor:**
- Самый быстрый способ: **Ctrl+`** (обратная кавычка)
- Или **Ctrl+Enter**

**Для открытия файла:**
- В SQL Editor: **Ctrl+O**
- Или перетащите файл в SQL Editor
