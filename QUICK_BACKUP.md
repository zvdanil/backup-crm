# Быстрое создание бэкапа (для бесплатного плана)

## ⚡ Самый быстрый способ (интерактивный скрипт)

**Для бесплатного плана Supabase нет автоматических бэкапов через Dashboard.**

Используйте интерактивный скрипт:

```powershell
.\create_backup_interactive.ps1
```

Скрипт:
1. Запросит пароль базы данных
2. Попытается создать бэкап через Supabase CLI
3. Если не получится, попробует через pg_dump
4. Сохранит бэкап в файл `backup_YYYYMMDD_HHMMSS.sql`

**Время выполнения:** ~3-5 минут

## 🔧 Через CLI (если нужен SQL дамп)

Для создания бэкапа через CLI нужен пароль базы данных. 

**Чтобы получить пароль:**
1. Откройте Dashboard → Settings → Database
2. Найдите раздел "Connection string" 
3. Пароль указан в connection string или сбросьте его

**Затем выполните:**
```powershell
# Используйте скрипт
.\create_backup.ps1 -DatabasePassword "ваш_пароль"
```

## 📝 Примечание

Я не могу автоматически создать бэкап, так как:
- Требуется пароль базы данных (безопасность)
- Команда `supabase db dump` требует Docker Desktop
- PITR не включен для этого проекта

**Рекомендация:** Используйте Dashboard - это самый простой и надежный способ.
