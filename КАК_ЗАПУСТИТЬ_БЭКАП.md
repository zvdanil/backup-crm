# Как запустить скрипт создания бэкапа

## 📍 Где запускать

Скрипт нужно запустить в **PowerShell** из **корневой директории проекта**:

```
C:\Users\User\Downloads\crmbackup-cursor
```

## 🚀 Способ 1: Через терминал в Cursor/VS Code

1. Откройте терминал в Cursor (`` Ctrl+` `` или `View → Terminal`)
2. Убедитесь, что вы в корневой директории проекта
3. Выполните команду:

```powershell
.\create_backup_interactive.ps1
```

## 🚀 Способ 2: Через PowerShell напрямую

1. Откройте PowerShell (Win + X → Windows PowerShell)
2. Перейдите в директорию проекта:

```powershell
cd C:\Users\User\Downloads\crmbackup-cursor
```

3. Запустите скрипт:

```powershell
.\create_backup_interactive.ps1
```

## 🚀 Способ 3: Через проводник Windows

1. Откройте проводник Windows
2. Перейдите в папку: `C:\Users\User\Downloads\crmbackup-cursor`
3. Нажмите `Shift + ПКМ` (правая кнопка мыши) в пустом месте
4. Выберите **"Открыть окно PowerShell здесь"**
5. Выполните:

```powershell
.\create_backup_interactive.ps1
```

## ⚠️ Если появится ошибка о политике выполнения

Если увидите ошибку типа:
```
cannot be loaded because running scripts is disabled on this system
```

Выполните (требуются права администратора):

```powershell
Set-ExecutionPolicy -ExecutionPolicy RemoteSigned -Scope CurrentUser
```

Или просто запустите скрипт с обходом политики:

```powershell
powershell -ExecutionPolicy Bypass -File .\create_backup_interactive.ps1
```

## 📝 Что произойдет при запуске

1. Скрипт покажет информацию о проекте
2. **Попросит ввести пароль базы данных** (пароль не будет виден на экране)
3. Попытается создать бэкап
4. Сохранит файл `backup_YYYYMMDD_HHMMSS.sql` в текущей директории

## 🔑 Где взять пароль базы данных

1. Откройте: https://app.supabase.com/project/qtphickigswerhvintvh/settings/database
2. Найдите раздел **"Connection string"** или **"Database password"**
3. Если пароль забыт, нажмите **"Reset database password"**
4. Скопируйте пароль

## ✅ Проверка

После успешного создания бэкапа вы увидите:

```
✅ Бэкап успешно создан!
Файл: backup_20260209_143022.sql
Размер: X.XX MB
```

Файл будет находиться в той же директории, где вы запустили скрипт.
